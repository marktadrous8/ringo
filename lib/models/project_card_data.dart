import 'dart:ui';

class ProjectCardData {
  final String count;
  final String label;
  final Color color;
  final String svgIconPath;

  ProjectCardData({
    required this.count,
    required this.label,
    required this.color,
    required this.svgIconPath,
  });
}